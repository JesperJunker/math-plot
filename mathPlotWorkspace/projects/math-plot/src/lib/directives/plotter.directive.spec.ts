import { PlotterDirective } from './plotter.directive';

describe('PlotterDirective', () => {
  it('should create an instance', () => {
    const directive = new PlotterDirective();
    expect(directive).toBeTruthy();
  });
});
